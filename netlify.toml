[build]
  command = "npm install -g pnpm --force && pnpm install && pnpm run build:libs && pnpm run build:employee"
  publish = "apps/employee-pwa/dist"
  base = ""

[build.environment]
  NPM_FLAGS = "--no-audit --no-fund"
  PNPM_FLAGS = "--shamefully-hoist"
  NODE_VERSION = "20"

